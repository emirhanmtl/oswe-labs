[
  {
    "vuln": "Authentication Bypass – SQL Injection",
    "pattern": "(SELECT|select)\\s+.*\\s+FROM\\s+.*WHERE\\s+.*(username|user)\\s*=\\s*['\"].*['\"]",
    "description": "Possible SQL injection in authentication query allowing bypass."
  },
  {
    "vuln": "Authentication Bypass – Weak Token Generation",
    "pattern": "\\b(rand|mt_rand)\\s*\\(",
    "description": "Use of predictable functions for token generation susceptible to guessing."
  },
  {
    "vuln": "Authentication Bypass – Type Juggling (PHP == Operator)",
    "pattern": "==\\s*\\$?\\w+\\s*==",
    "description": "Use of PHP '==' operator in auth checks can lead to type juggling bypass."
  },
  {
    "vuln": "Authentication Bypass – ATutor SQL Injection",
    "pattern": "(SELECT|select).*ATutor.*WHERE.*",
    "description": "Possible ATutor specific SQL injection for auth bypass."
  },
  {
    "vuln": "Authentication Bypass – Weak hash_hmac usage",
    "pattern": "hash_hmac\\s*\\(.*\\$?\\w+.*\\)",
    "description": "Potential predictable HMAC usage may allow tampering or bypass."
  },
  {
    "vuln": "Privilege Escalation – Persistent XSS via User Input",
    "pattern": "(echo|print)\\s*\\$?\\w*\\[['\"]description['\"]\\]",
    "description": "User input rendered without sanitization in 'description' field — persistent XSS risk."
  },
  {
    "vuln": "Remote Code Execution – Server Side Template Injection (Smarty)",
    "pattern": "\\$smarty->assign\\s*\\(.*motd.*\\)|\\$smarty->display\\s*\\(",
    "description": "User input passed directly into Smarty template — possible SSTI."
  },
  {
    "vuln": "Remote Code Execution – File Upload Bypass",
    "pattern": "move_uploaded_file\\s*\\(|\\$_FILES\\s*\\[.*\\]|pathinfo\\s*\\(.*extension.*",
    "description": "Unvalidated file upload handling allowing upload of executable files."
  },
  {
    "vuln": "Remote Code Execution – PHP Deserialization",
    "pattern": "unserialize\\s*\\(\\s*\\$?_?(POST|GET|REQUEST|COOKIE)\\[.*\\]\\)",
    "description": "Untrusted input passed to unserialize() — may lead to code execution."
  },
  {
    "vuln": "Remote Code Execution – SSRF",
    "pattern": "file_get_contents\\s*\\(\\s*\\$_(GET|POST|REQUEST).*http",
    "description": "Potential SSRF via fetching user controlled URLs."
  },
  {
    "vuln": "Remote Code Execution – Upload + SQLi Chaining",
    "pattern": "move_uploaded_file\\s*\\(.*|mysqli_query\\s*\\(.*\\$_(GET|POST)",
    "description": "File upload handling and SQLi patterns that may chain to RCE."
  },
  {
    "vuln": "File Inclusion – Local File Inclusion (LFI)",
    "pattern": "include\\s*\\(.*\\$_(GET|POST).*\\)",
    "description": "Potential Local File Inclusion via unsanitized include()."
  },
  {
    "vuln": "SQL Truncation / Injection",
    "pattern": "SUBSTRING\\s*\\(|LIKE\\s*'.*%'",
    "description": "Possible SQL truncation or wildcard injection leading to auth bypass or data leakage."
  },
  {
    "vuln": "RCE – DotNetNuke Cookie Deserialization",
    "pattern": "unserialize\\s*\\(\\s*\\$_COOKIE\\[.*\\]\\)",
    "description": "DotNetNuke insecure cookie deserialization — RCE vector."
  },
  {
    "vuln": "Deserialization – PHP Object Injection",
    "pattern": "unserialize\\s*\\(.*\\)",
    "description": "Potential object injection via unserialize()."
  },
  {
    "vuln": "File Upload Bypass – .phar",
    "pattern": "\\.phar",
    "description": "Possible .phar file upload bypass enabling RCE."
  },
  {
    "vuln": "SSRF – Fetching URLs",
    "pattern": "file_get_contents\\s*\\(.*\\$_(GET|POST).*\\)",
    "description": "Potential SSRF via fetching user-controlled URLs."
  },
  {
    "vuln": "Remote Code Execution – Command Injection",
    "pattern": "(system|exec|passthru|shell_exec|popen|proc_open)\\s*\\(",
    "description": "Direct execution of system commands. Critical risk if user input is passed."
  },
  {
    "vuln": "Authentication Bypass – PostgreSQL Injection",
    "pattern": "pg_query\\s*\\(.*\\$_(GET|POST|REQUEST)",
    "description": "Potential SQL Injection in PostgreSQL query using user input."
  },
  {
    "vuln": "File Inclusion – Dynamic Include",
    "pattern": "(include|require|include_once|require_once)\\s*\\(.*\\$_(GET|POST|REQUEST)",
    "description": "Dynamic file inclusion using user input, leading to LFI/RCE."
  }
]