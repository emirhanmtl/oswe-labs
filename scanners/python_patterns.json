[
  {
    "vuln": "Authentication Bypass – Weak Password Check",
    "pattern": "if\\s+.*password.*==.*['\"].*['\"]",
    "description": "Hardcoded or weak password check may allow authentication bypass."
  },
  {
    "vuln": "Authentication Bypass – Insecure Token Generation",
    "pattern": "(random\\.random|secrets\\.token_hex|os\\.urandom)",
    "description": "Use of insecure random functions for token generation may be predictable."
  },
  {
    "vuln": "Privilege Escalation – Unsanitized Input in Render",
    "pattern": "render_template\\s*\\(.*request\\.args.*\\)",
    "description": "Passing user input directly to templates without sanitization — SSTI risk."
  },
  {
    "vuln": "Remote Code Execution – Pickle Deserialization",
    "pattern": "pickle\\.loads\\s*\\(",
    "description": "Untrusted input deserialized with pickle leading to RCE."
  },
  {
    "vuln": "Remote Code Execution – Eval Usage",
    "pattern": "\\beval\\s*\\(",
    "description": "Use of eval() on user input can lead to RCE."
  },
  {
    "vuln": "Remote Code Execution – OS Command Injection",
    "pattern": "(os\\.system|subprocess\\.Popen|subprocess\\.call)\\s*\\(",
    "description": "Potential OS command injection if user input is passed without sanitization."
  },
  {
    "vuln": "Remote Code Execution – Exec Usage",
    "pattern": "\\bexec\\s*\\(",
    "description": "Use of exec() on user input can lead to RCE."
  },
  {
    "vuln": "Server Side Template Injection (SSTI)",
    "pattern": "render_template_string\\s*\\(",
    "description": "Rendering strings as templates (render_template_string) with user input is a critical SSTI risk."
  }
]