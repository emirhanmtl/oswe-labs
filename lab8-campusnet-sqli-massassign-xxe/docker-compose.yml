services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root_pw
      MYSQL_DATABASE: campusnet
      MYSQL_USER: campusnet
      MYSQL_PASSWORD: campusnet_pw
    volumes:
      - ./db/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot_pw"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build: ./app
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_NAME: campusnet
      DB_USER: campusnet
      DB_PASSWORD: campusnet_pw
    ports:
      - "8088:8080"
    depends_on:
      db:
        condition: service_healthy
